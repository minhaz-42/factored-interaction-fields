# factored-interaction-fields
Estimating the SHOW3D joint-anchored hand-object interaction field through its geometric arguments: hand joints, 6-DoF object pose and a known mesh. Code, derived labels, checkpoints and the HOT3D-IF cross-dataset protocol.
